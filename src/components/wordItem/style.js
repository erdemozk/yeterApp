import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  itemWrapper: {
    padding: 15,
    borderWidth: 1,
    borderRadius: 4,
    marginVertical: 5,
    marginHorizontal: 10,
    justifyContent: 'center',
  },
});
